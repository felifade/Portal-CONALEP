export const W09 = {
  materia: "mths",
  weekId: "W09",
  days: [
    {
      id: "tue",
      label: "Martes — Conclusión App Inventor + Hola ESP32 (H1-H2)",
      purpose: "Cerrar el proyecto de App Inventor mostrando el funcionamiento al docente, y dar el primer paso con ESP32 simulando en Wokwi.",
      hours: [
        {
          time: "Hora 1",
          title: "Conclusión de la app móvil: demostración al docente",
          theory: "La semana pasada programamos la navegación completa, los mensajes Notifier, la pantalla de Evaluación y el botón de regreso. Hoy cerramos el ciclo: cada alumno demuestra al docente que su app funciona de extremo a extremo navegando todas las pantallas en el emulador o teléfono con MIT AI2 Companion.",
          notebook: "Título: Cierre de mi app de seguridad digital.\n1. Dibuja el mapa de navegación completo: Portada → Menu → las 4 pantallas → regreso.\n2. ¿Qué pantalla fue la más difícil de programar?\n3. ¿Qué mejorarías si tuvieras más tiempo?\n4. ¿Qué componente de App Inventor crees que podrías usar para mandar datos a una placa electrónica? (Pista: BluetoothClient)",
          practice: "1. Abrir el proyecto en App Inventor y conectar con MIT AI2 Companion o emulador.\n2. Navegar todas las rutas frente al docente:\n   - Portada → Menu → Contraseñas → consejo → Regresar\n   - Portada → Menu → WiFi → recomendación → Regresar\n   - Portada → Menu → Riesgos → aviso → Regresar\n   - Portada → Menu → Evaluación → respuesta Sí → respuesta No → Regresar\n3. Corregir cualquier error antes de la demostración.\n4. Quien termine antes: apoyar a un compañero que aún no tenga su app lista.",
          product: "App de seguridad digital demostrada y aprobada por el docente.",
          teacherNotes: "👨‍🏫 NOTA DOCENTE: Revisión rápida uno por uno — el criterio es que las pantallas naveguen correctamente y los Notifier muestren texto. No es necesario que sea perfecto en diseño. Quien no terminó la semana pasada tiene esta hora para completar lo mínimo. Mantener el ritmo para arrancar Wokwi en H2."
        },
        {
          time: "Hora 2",
          title: "ESP32 en Wokwi: primer proyecto",
          theory: "El ESP32 es un microcontrolador moderno con WiFi y Bluetooth integrados en el mismo chip. A diferencia del Arduino Uno que necesita un módulo HC-05 externo para Bluetooth, el ESP32 ya lo trae — esto significa menos cableado, menos componentes y menos puntos de falla. Wokwi es un simulador web que permite programar y probar circuitos con ESP32 sin necesitar la placa física, ideal para aprender la lógica antes de trabajar con hardware real.",
          notebook: "Título: ESP32 vs Arduino Uno.\n1. Escribe 3 diferencias entre el ESP32 y el Arduino Uno.\n2. ¿Qué ventaja tiene el ESP32 para proyectos Bluetooth?\n3. ¿Qué es Wokwi y para qué lo usaremos?\n4. Dibuja la placa ESP32 e identifica: pines GPIO, GND, 3.3V y el puerto USB.",
          practice: "1. Abrir wokwi.com en el navegador.\n2. Iniciar sesión con Google.\n3. Clic en New Project → seleccionar ESP32.\n4. En el panel de simulación, clic en '+' y agregar un LED y una resistencia de 220Ω.\n5. Conectar: GPIO 2 → resistencia → LED(+) → GND.\n6. En el editor escribir el sketch Blink para el pin 2.\n7. Presionar ▶ y verificar que el LED parpadea.\n8. Cambiar el valor de delay() y observar el efecto.",
          code: "// PROGRAMA 01. Encender LED\nconst int LED_PIN = 2;\n\nvoid setup(){\n  pinMode(LED_PIN, OUTPUT);\n}\n\nvoid loop(){\n  digitalWrite(LED_PIN, HIGH); //encender\n  delay(500);\n  digitalWrite(LED_PIN, LOW);  //apagar\n  delay(500);\n}",
          product: "Proyecto creado en Wokwi con LED parpadeando en ESP32. Primera interacción con la plataforma de simulación.",
          teacherNotes: "👨‍🏫 NOTA DOCENTE: El error más común es no conectar el GND del LED al GND de la placa. Si el LED no enciende, pedir que sigan el cable desde el pin 2 hasta el LED y de ahí hasta GND. Dejar que lo descubran solos antes de intervenir."
        }
      ],
      cierre: "Cerraste tu primera app móvil y encendiste tu primer LED en ESP32. Esta semana la tecnología cambió de pantalla a circuito.",
      frase_docente: "Construir algo que funciona es la mejor forma de aprender."
    },
    {
      id: "wed",
      label: "Miércoles — Continuación Martes + Semáforo ESP32",
      purpose: "Cerrar la práctica del martes con el reto de delay y escalar el proyecto a un semáforo de 3 LEDs controlado por comandos desde el Serial Monitor.",
      hours: [
        {
          time: "Hora 1",
          title: "Continuación: control de LED + reto de delay",
          theory: "La función delay() recibe el tiempo en milisegundos — no en segundos. Para convertir: segundos × 1000 = milisegundos. Así, 2 segundos son 2000 ms, 5 segundos son 5000 ms y 10 segundos son 10000 ms. Esta conversión es fundamental porque todos los tiempos en Arduino/ESP32 se manejan en milisegundos.",
          notebook: "Título: delay() en milisegundos.\n1. ¿Cuántos milisegundos tiene 1 segundo?\n2. Completa la tabla:\n   - 2 segundos  = _____ ms → delay(_____)\n   - 5 segundos  = _____ ms → delay(_____)\n   - 10 segundos = _____ ms → delay(_____)\n3. ¿Qué pasaría si escribes delay(2) en lugar de delay(2000)?\n4. ¿Por qué crees que Arduino usa milisegundos en lugar de segundos?",
          practice: "1. Abrir el proyecto Wokwi del martes (Programa 01 — blink con delay 500ms).\n2. Verificar que el LED sigue parpadeando correctamente.\n3. Reto de delay — modificar el loop para que el LED haga 3 ciclos con tiempos distintos:\n   - Ciclo 1: encendido 2 segundos, apagado 2 segundos.\n   - Ciclo 2: encendido 5 segundos, apagado 5 segundos.\n   - Ciclo 3: encendido 10 segundos, apagado 10 segundos.\n4. Calcular primero en la libreta cuántos ms son cada tiempo antes de escribir el código.\n5. Presionar ▶ y medir con el reloj del celular si los tiempos son correctos.\n6. Guardar como 'Programa_02_Delay'.",
          code: "// PROGRAMA 02 — Reto de delay\n// Modifica los valores de delay() con los ms correctos\n\nconst int LED_PIN = 2;\n\nvoid setup(){\n  pinMode(LED_PIN, OUTPUT);\n}\n\nvoid loop(){\n  // Ciclo 1 — 2 segundos (2 s = _____ ms)\n  digitalWrite(LED_PIN, HIGH);\n  delay( );\n  digitalWrite(LED_PIN, LOW);\n  delay( );\n\n  // Ciclo 2 — 5 segundos (5 s = _____ ms)\n  digitalWrite(LED_PIN, HIGH);\n  delay( );\n  digitalWrite(LED_PIN, LOW);\n  delay( );\n\n  // Ciclo 3 — 10 segundos (10 s = _____ ms)\n  digitalWrite(LED_PIN, HIGH);\n  delay( );\n  digitalWrite(LED_PIN, LOW);\n  delay( );\n}",
          codeRef: "// PROGRAMA 02 — Solución\n\nconst int LED_PIN = 2;\n\nvoid setup(){\n  pinMode(LED_PIN, OUTPUT);\n}\n\nvoid loop(){\n  // Ciclo 1 — 2 segundos\n  digitalWrite(LED_PIN, HIGH);\n  delay(2000);\n  digitalWrite(LED_PIN, LOW);\n  delay(2000);\n\n  // Ciclo 2 — 5 segundos\n  digitalWrite(LED_PIN, HIGH);\n  delay(5000);\n  digitalWrite(LED_PIN, LOW);\n  delay(5000);\n\n  // Ciclo 3 — 10 segundos\n  digitalWrite(LED_PIN, HIGH);\n  delay(10000);\n  digitalWrite(LED_PIN, LOW);\n  delay(10000);\n}",
          product: "Programa_02_Delay guardado en Wokwi con los 3 ciclos de 2, 5 y 10 segundos verificados con el reloj del celular.",
          teacherNotes: "👨‍🏫 NOTA DOCENTE: Pedir que calculen en la libreta antes de escribir el código — la tabla de la libreta ya la tienen. El error más común es delay(2), delay(5), delay(10) sin multiplicar. Dejar que lo vean en el simulador: el LED parpadea tan rápido que parece siempre encendido. Quien termine antes: hacer que el LED parpadee exactamente 1 minuto en cada ciclo (delay(60000))."
        },
        {
          time: "Hora 2",
          title: "Semáforo con 3 LEDs controlado por Serial Monitor",
          theory: "Un semáforo solo puede mostrar un estado a la vez — rojo, amarillo o verde. En código esto se traduce en: apagar todos los LEDs y encender solo el que corresponde al comando recibido. El Serial Monitor actúa hoy como el teléfono: envía un carácter y el ESP32 reacciona. Para controlar 3 LEDs independientes necesitamos 3 pines GPIO diferentes y una función apagar_todos() que garantice que solo un LED esté activo en cada momento.",
          notebook: "Título: Semáforo con ESP32.\n1. Dibuja el circuito: ESP32 con 3 LEDs (rojo pin 25, amarillo pin 26, verde pin 27).\n2. ¿Por qué hay que apagar todos los LEDs antes de encender el que corresponde?\n3. ¿Qué pasa si no apagas los otros antes de encender el nuevo?\n4. Escribe la tabla de comandos: R → LED rojo, Y → LED amarillo, G → LED verde.\n5. ¿Qué reemplazaría al Serial Monitor cuando tengamos el ESP32 físico?",
          practice: "1. Crear un nuevo proyecto en Wokwi → seleccionar ESP32.\n2. Agregar 3 LEDs (rojo, amarillo, verde) con sus resistencias 220Ω.\n3. Conectar: LED rojo → pin 25, LED amarillo → pin 26, LED verde → pin 27.\n4. Escribir el sketch del semáforo.\n5. Presionar ▶ y abrir el Serial Monitor.\n6. Probar cada comando:\n   - Escribir 'R' → solo LED rojo enciende.\n   - Escribir 'Y' → solo LED amarillo enciende.\n   - Escribir 'G' → solo LED verde enciende.\n7. Verificar que al cambiar de color el anterior se apaga.\n8. Guardar el proyecto como 'Semaforo_Serial_v1'.",
          code: "const int LED_ROJO     = 25;\nconst int LED_AMARILLO = 26;\nconst int LED_VERDE    = 27;\n\nvoid setup() {\n  Serial.begin(115200);\n  pinMode(LED_ROJO,     );\n  pinMode(LED_AMARILLO, );\n  pinMode(LED_VERDE,    );\n  Serial.println(\"R=Rojo  Y=Amarillo  G=Verde\");\n}\n\nvoid apagar_todos() {\n  digitalWrite(LED_ROJO,     LOW);\n  digitalWrite(LED_AMARILLO, LOW);\n  digitalWrite(LED_VERDE,    LOW);\n}\n\nvoid loop() {\n  if (Serial.available()) {\n    char cmd = Serial.read();\n    apagar_todos();\n\n    if      (cmd == '') digitalWrite(LED_ROJO,     HIGH);\n    else if (cmd == '') digitalWrite(LED_AMARILLO, HIGH);\n    else if (cmd == '') digitalWrite(LED_VERDE,    HIGH);\n\n    Serial.print(\"Comando: \");\n    Serial.println(cmd);\n  }\n}",
          codeRef: "const int LED_ROJO     = 25;\nconst int LED_AMARILLO = 26;\nconst int LED_VERDE    = 27;\n\nvoid setup() {\n  Serial.begin(115200);\n  pinMode(LED_ROJO,     OUTPUT);\n  pinMode(LED_AMARILLO, OUTPUT);\n  pinMode(LED_VERDE,    OUTPUT);\n  Serial.println(\"R=Rojo  Y=Amarillo  G=Verde\");\n}\n\nvoid apagar_todos() {\n  digitalWrite(LED_ROJO,     LOW);\n  digitalWrite(LED_AMARILLO, LOW);\n  digitalWrite(LED_VERDE,    LOW);\n}\n\nvoid loop() {\n  if (Serial.available()) {\n    char cmd = Serial.read();\n    apagar_todos();\n\n    if      (cmd == 'R') digitalWrite(LED_ROJO,     HIGH);\n    else if (cmd == 'Y') digitalWrite(LED_AMARILLO, HIGH);\n    else if (cmd == 'G') digitalWrite(LED_VERDE,    HIGH);\n\n    Serial.print(\"Comando: \");\n    Serial.println(cmd);\n  }\n}",
          product: "Semáforo de 3 LEDs funcional en Wokwi respondiendo a comandos R, Y, G desde el Serial Monitor. Proyecto guardado.",
          teacherNotes: "👨‍🏫 NOTA DOCENTE: La función apagar_todos() es el concepto clave — enseña a resetear el estado antes de aplicar el nuevo. Si alguien pregunta por qué no solo encender el nuevo sin apagar, demostrar en vivo qué pasa: los LEDs quedan encendidos simultáneamente. Quien termine antes: agregar un comando 'A' que haga el ciclo automático rojo→amarillo→verde con delay(1000) dentro de un else."
        }
      ],
      cierre: "Convertiste milisegundos en segundos reales y escalaste de 1 LED a 3. La lógica de estado del semáforo es exactamente la que usará el Bluetooth.",
      frase_docente: "Resetear antes de actuar — en código y en la vida."
    },
    {
      id: "thu",
      label: "Jueves — Control de LED por Serial Monitor",
      purpose: "Implementar y depurar el control de un LED mediante comandos de texto enviados desde el Serial Monitor, entendiendo cómo el ESP32 lee, interpreta y responde a cada instrucción.",
      hours: [
        {
          time: "Hora 1",
          title: "Práctica 1: Control de LED por comandos Serial",
          theory: "El Serial Monitor funciona como un canal de comunicación entre la computadora y el ESP32. Serial.begin(115200) abre ese canal a 115200 baudios — si el monitor tiene una velocidad diferente, los caracteres llegan como basura. Serial.available() devuelve cuántos bytes están esperando en el buffer: si es mayor a 0, hay un dato listo. Serial.read() toma ese byte y lo guarda como un char (carácter). El programa compara ese carácter con '1', '0' o 'p' y ejecuta la acción correspondiente — exactamente como lo hará el Bluetooth más adelante.",
          notebook: "Título: Control de LED por Serial.\nCopia y explica cada instrucción:\n1. Serial.begin(115200) → ________________________________\n2. pinMode(LED_PIN, OUTPUT) → ________________________________\n3. digitalWrite(LED_PIN, LOW) → ________________________________\n4. if (Serial.available()) → ________________________________\n5. char comando = Serial.read() → ________________________________\n6. if (comando == '1') → ________________________________\n7. for (int i = 0; i < 3; i++) → ________________________________\n8. ¿Por qué ignoramos '\\n', '\\r' y ' '? → ________________________________",
          practice: "1. Abrir Wokwi y crear un nuevo proyecto ESP32.\n2. Agregar un LED con resistencia 220Ω conectado al GPIO 2.\n3. Copiar el código de Práctica 1 en el editor.\n4. Presionar ▶ para iniciar la simulación.\n5. Abrir el Serial Monitor — verificar que la velocidad es 115200.\n6. Si aparece texto ilegible (??? o símbolos raros): el baud rate está mal — corregirlo.\n7. Probar los 3 comandos:\n   - Enviar '1' → LED enciende + mensaje '✓ LED encendido'\n   - Enviar '0' → LED apaga + mensaje '✓ LED apagado'\n   - Enviar 'p' → LED parpadea 3 veces + mensajes\n8. Probar un comando inválido (por ejemplo 'x') → verificar que aparece '✗ Comando desconocido'\n9. Guardar el proyecto como 'Control_LED_Serial_P1'.",
          code: "// Práctica 1 — Control de LED por comandos\n// Completa los espacios en blanco\n\nconst int LED_PIN = ;  // ¿qué pin?\n\nvoid setup() {\n  Serial.begin( );      // ¿cuántos baudios?\n  pinMode(LED_PIN, );\n  digitalWrite(LED_PIN, );  // empieza apagado\n\n  Serial.println(\"Envía '1' para encender el LED\");\n  Serial.println(\"Envía '0' para apagar el LED\");\n  Serial.println(\"Envía 'p' para parpadear 3 veces\");\n}\n\nvoid loop() {\n  if (Serial.    ()) {       // ¿hay datos?\n    char comando = Serial.  ();\n\n    if (comando == ' ') {          // encender\n      digitalWrite(LED_PIN, HIGH);\n      Serial.println(\"✓ LED encendido\");\n    } \n    else if (comando == ' ') {     // apagar\n      digitalWrite(LED_PIN, LOW);\n      Serial.println(\"✓ LED apagado\");\n    }\n    else if (comando == ' ') {     // parpadear\n      for (int i = 0; i < 3; i++) {\n        digitalWrite(LED_PIN, HIGH);\n        delay(300);\n        digitalWrite(LED_PIN, LOW);\n        delay(300);\n      }\n    }\n  }\n}",
          codeRef: "// Práctica 1 — Control de LED por comandos\n// Versión Serial (simula el Bluetooth para aprender en Wokwi)\n\nconst int LED_PIN = 2;  // GPIO donde está conectado el LED\n\nvoid setup() {\n  Serial.begin(115200);           // Inicia comunicación serial a 115200 baudios\n  pinMode(LED_PIN, OUTPUT);       // Configura el pin como salida\n  digitalWrite(LED_PIN, LOW);     // El LED empieza apagado\n\n  Serial.println(\"=================================\");\n  Serial.println(\"  Control de LED - Práctica 1\");\n  Serial.println(\"=================================\");\n  Serial.println(\"Envía '1' para encender el LED\");\n  Serial.println(\"Envía '0' para apagar el LED\");\n  Serial.println(\"Envía 'p' para parpadear 3 veces\");\n  Serial.println(\"---------------------------------\");\n}\n\nvoid loop() {\n  if (Serial.available()) {\n    char comando = Serial.read();\n\n    if (comando == '1') {\n      digitalWrite(LED_PIN, HIGH);\n      Serial.println(\"✓ LED encendido\");\n    } \n    else if (comando == '0') {\n      digitalWrite(LED_PIN, LOW);\n      Serial.println(\"✓ LED apagado\");\n    }\n    else if (comando == 'p') {\n      Serial.println(\"✓ Parpadeando...\");\n      for (int i = 0; i < 3; i++) {\n        digitalWrite(LED_PIN, HIGH);\n        delay(300);\n        digitalWrite(LED_PIN, LOW);\n        delay(300);\n      }\n      Serial.println(\"  ...listo\");\n    }\n    else if (comando != '\\n' && comando != '\\r' && comando != ' ') {\n      Serial.print(\"✗ Comando desconocido: '\");\n      Serial.print(comando);\n      Serial.println(\"'\");\n    }\n  }\n}",
          product: "Proyecto Control_LED_Serial_P1 funcionando en Wokwi: los 3 comandos responden correctamente y el Serial Monitor muestra los mensajes de confirmación.",
          teacherNotes: "👨‍🏫 NOTA DOCENTE: El problema más frecuente es que el Serial Monitor esté en 9600 baudios en lugar de 115200 — los caracteres llegan como '???'. Pedir que lo corrijan antes de empezar a depurar el código. El segundo error común: no abrir el Serial Monitor después de iniciar la simulación (el menú de Wokwi). Recordar que '\\n' y '\\r' son los caracteres invisibles que agrega el Enter — sin ignorarlos, el ESP32 responde 'comando desconocido' después de cada comando válido."
        },
        {
          time: "Hora 2",
          title: "Práctica 2: Dos LEDs independientes",
          theory: "Cuando conectamos varios LEDs a distintos pines GPIO, cada uno se declara con su propia variable y se configura por separado en el setup(). Lo importante es que los comandos ahora deben identificar a CUÁL LED afectar — por eso '1' controla LED1, '2' controla LED2, 'b' los enciende a ambos y '0' apaga todo. Este patrón de múltiples salidas es la base del semáforo y de cualquier sistema con más de un actuador.",
          notebook: "Título: Dos LEDs, dos pines, un programa.\n1. ¿En qué pin va LED1 y en qué pin va LED2? → ________________________________\n2. ¿Qué comando enciende solo LED1? → ________________________________\n3. ¿Qué comando enciende solo LED2? → ________________________________\n4. ¿Qué hace el comando 'b'? → ________________________________\n5. ¿Qué hace el comando '0'? → ________________________________\n6. Si envías '1' y luego '2' sin enviar '0', ¿qué LEDs están encendidos? → ________________________________",
          practice: "1. Mantener el LED1 del GPIO 2 del proyecto anterior.\n2. Agregar un segundo LED con resistencia 220Ω conectado al GPIO 4.\n3. Copiar el código de Práctica 2 en el editor.\n4. Presionar ▶ y abrir el Serial Monitor (115200 baudios).\n5. Probar los 4 comandos en orden:\n   - Enviar '1' → solo LED1 enciende\n   - Enviar '2' → solo LED2 enciende\n   - Enviar 'b' → ambos LEDs encienden\n   - Enviar '0' → ambos LEDs apagan\n6. Probar combinaciones: '1' luego '2' — ¿qué pasa?\n7. Guardar como 'Control_LED_Serial_P2'.",
          code: "// Práctica 2 — Dos LEDs independientes\n// Completa los espacios en blanco\n\nconst int LED1 = 2;\nconst int LED2 = ;   // ¿qué pin?\n\nvoid setup() {\n  Serial.begin(115200);\n  pinMode(LED1, OUTPUT);\n  pinMode(LED2, );\n  digitalWrite(LED1, LOW);\n  digitalWrite(LED2, );\n  Serial.println(\"'1'=LED1  '2'=LED2  'b'=ambos  '0'=todo apagado\");\n}\n\nvoid loop() {\n  if (Serial.available()) {\n    char cmd = Serial.read();\n\n    if (cmd == '1') {\n      digitalWrite(LED1, );\n      Serial.println(\"✓ LED1 encendido\");\n    }\n    else if (cmd == '2') {\n      digitalWrite(LED2, );\n      Serial.println(\"✓ LED2 encendido\");\n    }\n    else if (cmd == 'b') {\n      digitalWrite(LED1, );\n      digitalWrite(LED2, );\n      Serial.println(\"✓ Ambos encendidos\");\n    }\n    else if (cmd == '0') {\n      digitalWrite(LED1, LOW);\n      digitalWrite(LED2, );\n      Serial.println(\"✓ Todo apagado\");\n    }\n  }\n}",
          codeRef: "// Práctica 2 — Dos LEDs independientes\n\nconst int LED1 = 2;\nconst int LED2 = 4;\n\nvoid setup() {\n  Serial.begin(115200);\n  pinMode(LED1, OUTPUT);\n  pinMode(LED2, OUTPUT);\n  digitalWrite(LED1, LOW);\n  digitalWrite(LED2, LOW);\n  Serial.println(\"'1'=LED1  '2'=LED2  'b'=ambos  '0'=todo apagado\");\n}\n\nvoid loop() {\n  if (Serial.available()) {\n    char cmd = Serial.read();\n\n    if (cmd == '1') {\n      digitalWrite(LED1, HIGH);\n      Serial.println(\"✓ LED1 encendido\");\n    }\n    else if (cmd == '2') {\n      digitalWrite(LED2, HIGH);\n      Serial.println(\"✓ LED2 encendido\");\n    }\n    else if (cmd == 'b') {\n      digitalWrite(LED1, HIGH);\n      digitalWrite(LED2, HIGH);\n      Serial.println(\"✓ Ambos encendidos\");\n    }\n    else if (cmd == '0') {\n      digitalWrite(LED1, LOW);\n      digitalWrite(LED2, LOW);\n      Serial.println(\"✓ Todo apagado\");\n    }\n  }\n}",
          product: "Proyecto Control_LED_Serial_P2 con dos LEDs respondiendo a comandos independientes desde el Serial Monitor.",
          teacherNotes: "👨‍🏫 NOTA DOCENTE: La pregunta clave de la práctica 6 (enviar '1' luego '2') revela si los alumnos entienden que los comandos no apagan los demás LEDs — ambos quedan encendidos. Esto introduce la necesidad de 'apagar todo antes de encender' que usará el semáforo. Quien termine antes: agregar un comando 'a' que alterne los LEDs (si LED1 está encendido → apagarlo y encender LED2, y viceversa)."
        }
      ],
      cierre: "De 1 LED a 2, de 3 comandos a 4. Cada nuevo pin es un nuevo actuador que el ESP32 puede controlar de forma independiente.",
      frase_docente: "Un programa que escucha es más poderoso que uno que solo ejecuta."
    }
  ]
};
